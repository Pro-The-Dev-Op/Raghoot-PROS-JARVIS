pywhatkit (pip install pywhatkit)
pyaudio(pip install pipwin the write pipwin install pyaudio)
pyttsx3(pip install pyttsx3)
wikipedia(pip install wikipedia)
speech_recognition(pip install speech recognition)
